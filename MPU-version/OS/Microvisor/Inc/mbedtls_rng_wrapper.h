/* Define to prevent recursive inclusion -------------------------------------*/
#ifndef __MBEDTLS_RNG_WRAPPER_H
#define __MBEDTLS_RNG_WRAPPER_H

#include "stm32l4xx_hal.h"

/* Define the function to create an interface beween the boord ranom generator and the mbedtls library */
//HAL_StatusTypeDef mbedtls_rng_init();
//HAL_StatusTypeDef mbedtls_rng_deinit();
//int mbedtls_rng_f_source(void *data, unsigned char *output, size_t len, size_t *olen);


#endif /* __MBEDTLS_RNG_WRAPPER_H */